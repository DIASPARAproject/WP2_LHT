Life history trait models of eel and salmon (deliverable 2.2)

The model_species_trait includes code for building the model in Nimble. 

The summary_species_trait includes code for reading and defining data, plotting the modeled trait data, model convergence assessment and resulting output from the models. 